# GPS 路線搜尋 — 可行性分析

**問題：** 可否把紅巴的文字搜尋擴展到巴士及專線小巴，並以使用者的 GPS 位置推薦合適路線？

**結論：可行性高（巴士 ✅ 完全可行／專線小巴 ✅ 可行／紅巴 ❌ 不可行）。**
**不需要新資料、不需要 API key、可完全離線運作**，因為所需的座標資料已經在你現時
已經載入的 `routeFareList.min.json` 之內。

---

## 現況（2026-09-24 更新）

本文件是**當初的可行性分析**，實作結果與原本構想有兩處不同，讀後續章節時請留意：

| 項目 | 原本構想 | 實際實作 |
|---|---|---|
| 入口 | 一個「附近路線」按鈕（純 GPS，無目的地） | **已移除按鈕**；併入「查詢」→ `placeSearch()` → `findStopsToDestination()` |
| 查詢語意 | 「我附近有甚麼路線」 | 「**我附近哪 5 個車站有車去我輸入的目的地**」 |
| 適用頁面 | 只有巴士頁 | 巴士頁**與專線小巴頁**（`gmb.html` 同一個做法） |

另外新增了一個原本沒提到的必要條件：**方向必須正確**。
候選車站必須在「同一路線、同一方向」上排在目的地之前（用站序判斷），
否則中環會推薦「往屯門方向」的路線去旺角。詳見 README 的「目的地搜尋」一節。

> 小巴版的站序取自 `routeStopIdx[].pos`，而**不能**只看路線編號：
> 小巴的 `gtfsId`（＝GMB API `route_id`）同時涵蓋去回程，
> 772 個 `gtfsId` 中有 382 個對應兩個 `bound`。詳見 README。

另外，第 4 節提到「同一個路線號會出現兩次（去程／回程），顯示時需要合併」，
實作時比原本設想更麻煩：**同一個路線號亦可能分屬不同地區，票價也不同**
（路線「1」港島 $11.8、九龍 $10.9）。所以小巴票價索引必須以 `gtfsId` 為主鍵，
否則 770 個 `gtfsId` 中有 113 個會顯示錯的票價。詳見 README「路線籤上的票價」。

結果面板的呈現方式亦與原本構想不同：桌機放在**右欄**（與「路線詳細資料」同一區塊），
只有手機才用全螢幕彈出層。詳見 README「結果面板的位置」。

小巴頁的路線搜尋又多了一層：因為 API 必須指定地區，而**479 個路線號之中有 87 個
跨地區**（其中 6 個三區都有），所以 `gmb.html` 改成「平常不顯示地區按鈕、
只在號碼重複時彈窗問」。詳見 README「輸入路線號之後」。
巴士頁不受影響 —— 桌機可一次查三間公司，本來就不必問。

第 7 節建議的 `findNearbyRoutes()` 已依本文件寫成並可用，但**目前已無呼叫點**
（被 `findStopsToDestination()` 取代），保留原因見 README。
`gmb.html` 亦同理：`findNearbyGmbRoutes()` 已無呼叫點。
其餘章節的資料探勘結果（車站數、覆蓋率、`stopMap` 反查表、GMB 對接）全部仍然成立。

---

## 1. 關鍵發現：`routeFareList.min.json` 不只是票價表

`index.html` 現時只用了它的 `routeList`（票價），**但其餘欄位一直被忽略**：

| 欄位 | 內容 | 對 GPS 搜尋的作用 |
|---|---|---|
| `stopList` | **15,267** 個車站，**全部**有 `location: {lat, lng}`（WGS84） | 計算你與車站的距離 |
| `stopMap` | **11,878** 個車站的跨公司對照表 | **把「距離近」連到「有咩路線」** |
| `routeList` | **3,766** 條路線，含 `stops: { 公司: [車站ID…] }` | 由車站反查路線 |
| `serviceDayMap` / `holidays` | 服務日、公眾假期 | 之後做「今日有無車」判斷 |

座標範圍：緯度 22.2053–22.5546、經度 113.8607–114.3714 — 正好覆蓋全香港。
**座標覆蓋率 100%，沒有一個車站缺少座標。**

### 各營辦商的車站覆蓋

| 營辦商 | 可連到座標的車站數 |
|---|---|
| 九巴 (kmb) | 9,331 |
| 專線小巴 (gmb) | 6,711 |
| 城巴 (ctb) | 6,293 |
| 輕鐵接駁 (lrtfeeder) | 1,205 |
| 嶼巴 (nlb) | 394 |
| 港鐵 (mtr) | 330 |
| 輕鐵 (lightRail) | 257 |

---

## 2. 實測結果（中環 22.2819, 114.1582）

以中環一個座標測試，**120 米內找到 12 個車站、96 條路線**，並正確橫跨
城巴 (ctb)、九巴 (kmb) 及專線小巴 (gmb)：

```
('1',   '中環 (港澳碼頭)', '跑馬地 (上)',   'ctb')
('10',  '北角碼頭',        '堅尼地城',      'ctb')
('101', '觀塘(裕民坊)',    '堅尼地城',      'kmb')
('1',   '中環(香港站)',    '山頂',          'gmb')
...
```

即係話：**用你已經有的檔案，今日就可以做到「你附近有咩路線」。**

---

## 3. ⚠️ 最容易做錯的一步（實作時務必注意）

`stopList` 的 key 是**內部雜湊 ID**（例如 `CBF110E4B3E23071`），
但 `routeList[].stops` 用的是**各公司自己的車站 ID**（例如 kmb 的 `CBF110E4B3E23071`、
ctb 的 `001566`）。兩者**不是同一套編號**。

> **我測試時第一次只用內部 ID 做距離比對，結果中環只找到 3 條路線。**
> **改用 `stopMap` 反查表（`公司 + 公司ID → 內部ID`）之後，就找到 96 條。**

所以必須先建立反查表：

```js
// 反查：公司 + 公司車站ID  ->  內部ID
const rev = {};
for (const [internalId, pairs] of Object.entries(stopMap)) {
  for (const [company, companyId] of pairs) {
    (rev[company] ||= {})[companyId] = internalId;
  }
}
```

這是整個功能的關鍵，做漏了就會「明明企喺中環但只搵到 3 條線」。

---

## 4. 專線小巴：可行，而且比 GMB 頁面現有資料更完整

`routeFareList.min.json` 內有 **1,154 條 GMB 路線**並附帶 `stops.gmb` 車站列表，
而 `gmb-detail.json` 只有 **570 條**。

兩者可以用 `gtfsId` 對上 `gmb-detail.json` 的 `td_route_id` — **848 條成功對接**。

- 好處：GPS 搜尋覆蓋面幾乎是 GMB 頁面現有路線的兩倍。
- 注意：fare 檔的 GMB 路線是**逐方向**一條（key 形如 `1+1+起點+終點`），
  所以同一個路線號會出現兩次（去程／回程），顯示時需要合併。
- 只有 848/1154 能對接 detail 檔，其餘可顯示路線但未必有逐站詳情。

---

## 5. 紅巴：不可行（維持文字搜尋）

`rmb-routes.json` **完全沒有座標**（無 `lat`/`lng`）、沒有車站清單，只有地區標籤。
紅巴本身亦無官方車站座標（README 已記錄此事實），所以維持現狀是正確做法。

## 6. `gmb-stops-coords.csv` 目前用不著

4,766 個車站，用 **HK1980 Grid**（Easting/Northing），需要轉換才成 WGS84；
而且：

- CSV **沒有車站名稱欄位**（只有 `STOP_ID` + 座標）
- 它的 `STOP_ID`（如 `20003351`）**在 `gmb-detail.json` 內完全找不到**

即係話現階段無法與任何現有資料對接。而且 fare 檔已提供更好的座標來源，
這個 CSV 基本上是**多餘**的（可考慮移入 `Trash/`）。

---

## 7. 建議實作方式

1. **擴充 `buildFareIndex()`**（約 `index.html` 第 967 行）：現時它只抽 `fares`，
   同時保留 `stopList`，並建立第 3 節的反查表。只做一次，之後查詢是 O(n) 掃描。
2. **取得定位**：`navigator.geolocation.getCurrentPosition()`。
3. **篩選**：用 haversine 公式取 300–500 米內的車站。
4. **反查路線**：附近車站的公司 ID → 反查表 → `routeList[].stops` 比對。
5. **排序顯示**：按（最近車站距離, 路線號）排序，重用現有搜尋結果 UI。

### 必須留意的限制（要誠實告知使用者）

- **定位需要 HTTPS 或 localhost。** 用 `file://` 開會失敗 — README 已有的警告同樣適用；
  用 `start-local.bat`（localhost）就無問題。
- **需要使用者授權**，被拒絕時要優雅降級（可退回輸入地點文字搜尋）。
- **「近」不等於「方向對」**：一個車站只服務單一方向，所以結果應該顯示
  **車站名／方向**，而不只是路線號碼，否則使用者可能搭錯方向。
- **8 MB 檔案**已經在載入；額外處理 `stopList`/`stopMap` 會增加少許啟動時間，
  建議 lazily 建立反查表（第一次用定位時才建）。

---

## 總結

| 模式 | GPS 搜尋 | 依據 |
|---|---|---|
| 巴士 | ✅ 可行 | 15,267 車站全部有座標；實測中環 120 米內 96 條路線 |
| 專線小巴 | ✅ 可行 | fare 檔有 1,154 條 GMB 路線連車站，848 條可對接 detail |
| 紅巴 | ❌ 不可行 | 資料完全無座標，亦無官方車站座標 |
| 渡輪 | ✅ 有少量資料 | `hkkf` 2 / `sunferry` 2 / `fortuneferry` 1 個車站 |

**核心優點：零新增依賴。** 所有座標資料已在 `routeFareList.min.json`，
只是目前未被使用。
